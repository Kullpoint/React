const Message = ({ name, price, emoji, soldOut }) => {
    const handleClick = () => {
        
    }

    return (
        <>
            <button onClick={}>Click here to get an message</button>
        </>
    );
};

export default Message;

